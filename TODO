licenses

lens
  return oracle prices

oracle provenance
  can use this: https://oracle-checks-data.euler.finance/1/adapters/all.json

UI bugs
  if asset is WETH and unit of account is WETH, it breaks. probably just don't install oracle in router
  when wallet not connected stuff breaks
    disable buttons etc
  decode LIQUIDATE history metadata
  ? when doing a repay(), prepend transition call to batch

UI screens
  bond create screen
    show creation summary in right column

  browse page
    filters
      borrow asset selector
      collateral asset selector
      minimum size selection
      term duration

  bond info page
    ! when doing repay, if max clicked, have a checkbox to withdraw all the collateral too
    oracle details, prices
    ? open interest

  settings screen
    add custom tokens
    ? add custom oracles

UI misc
  subaccount selection
  url parameters, like chain
  ? spy mode
