contract
  admin methods
  early repay penalty
    repay method in LayerCredit that pulls in repay amount+fee
      deposit the collected penalty into the bond vault on behalf of creator (works great if creator is earn vault)
  if restricted lender, don't require reserves
  when creating bonds, verify EVK impl hasn't changed since deployment
  ? all-subaccount history view (use entity with last byte zeroed out, or primary)
  ? variable interest rates
  ? let bond creator reduce reserve multiplier (<= max multiplier)
  ? should debt socialisation be on? how do reserved funds become junior
  ? secondary market for trading bond shares (separate contract)

  misc
    factor into multiple files, structured storage
    storage packing, code-size reduction
    tests

bugs
  if asset is WETH and unit of account is WETH, it breaks. probably just don't install oracle in router
  when wallet not connected stuff breaks
    disable buttons etc

docs

lens
  return oracle prices

UI
  homepage
    active bonds (sorted by totalSupply)
    global history
    news

  supply page
    filters
      supply asset selector
      minimum size selection
      term duration

  borrow page
    filters
      borrow asset selector
      collateral asset selector
      minimum size selection
      term duration
    list of bonds

  bond info page
    show restricted borrowers/lenders
    oracle details, prices
    touch/transition
      add transition batch item to repay
    when doing repay, if max clicked, have a checkbox to withdraw all the collateral too
    subscreen: suppliers/borrowers
      liquidate button
    subscreen: open interest

  my account screen
    current positions
      nav/ltv/leverage
    my history

  settings screen
    add custom tokens
    add custom oracles

  oracle integrity whitelist
  subaccount selector
  url parameters
  command bar
  ? spy mode
